# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code's local scratch: agent worktrees are whole checkouts of this
# repo, so a stray `git add .` would stage a nested clone. launch.json here
# is a personal dev convenience, not a shared config.
.claude/
